sudo apt update        #\
                         #- estos dos comandos conviene ponerlos regularmente para mantener el sistema actualizado
sudo apt full-upgrade  #/ 

sudo apt-get install libcamera-apps

sudo apt install python3-picamzero

sudo apt-get install cmake

sudo apt-get install vim 

python3 -m venv venv  #-  SOLO UNA VEZ (crea un virtual enviorment)

source venv/bin/activate  

pip3 install opencv-python

pip3 install imutils #Editar archivo webcamvideostream.py de la libreria y añadir variables width y height al __init__

pip3 install pyyaml

pip3 install wheel

pip3 install apriltag

pip3 install dt-apriltags

pip3 install transforms3d

pip3 install moms-apriltag
